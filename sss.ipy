# coding: utf-8
import MySQLdb
db = MySQLdb.connect(
    host='ktowin_db',
    user='root',
    password='root')
db = MySQLdb.connect(
    host='localhost',
    user='root',
    passwd='root',)
db = MySQLdb.connect(
    host='localhost',
    user='root',
    passwd='root',)
db = MySQLdb.connect(
    host='localhost',
    user='root',
    passwd='root',
    db='ktowin_db')
c = db.cursor()
c.execute("SHOW TABLES")
c.fetchall()
%gui
%gui wx
%gui tk
%save sss
%save -r  sss
